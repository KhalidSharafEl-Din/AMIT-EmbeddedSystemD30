#include <stdlib.h>
#include <stdio.h>

int main(){

    printf("Hello, Crule World!\n");
    // To print a number you have to use format specifier 
    printf("My favourite %s is %d", "number\n", 500);
    // This becomes useful when we have variables 
    double myNumber = 65.4 ;
    printf("This is a float number that we can do math on %s", myNumber);
    return 0; 
}
